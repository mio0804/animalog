# 認証モード設定
# 'true' = Cognito認証を使用
# 'false' = 開発用モック認証を使用
VITE_USE_COGNITO=false

# AWS Cognito設定（VITE_USE_COGNITO=trueの場合に必要）
VITE_COGNITO_USER_POOL_ID=your-user-pool-id
VITE_COGNITO_CLIENT_ID=your-client-id
VITE_COGNITO_DOMAIN=your-cognito-domain.auth.ap-northeast-1.amazoncognito.com
VITE_COGNITO_REDIRECT_URI=http://localhost:3000/callback
VITE_COGNITO_LOGOUT_URI=http://localhost:3000/login

# Vite開発サーバー設定
# バックエンドAPIのプロキシ先を指定（デフォルト: http://localhost:5000）
# VITE_API_PROXY_TARGET=http://localhost:5000